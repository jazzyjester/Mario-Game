import SwiftUI

// Placeholder app shell — replaced by the real TCA AppFeature in Phase 3.
@main
struct MarioAppMain: App {
  var body: some Scene {
    WindowGroup {
      Text("Mario Swift — coming soon")
        .padding()
    }
  }
}
